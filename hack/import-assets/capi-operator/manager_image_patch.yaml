apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: quay.io/asalkeld/openshift-cluster-api-operator-amd64:dev
          name: manager
